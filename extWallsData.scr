// extWallsData.scr

const HOUSE_WIDTH = 40;    // Width of the house in feet
const HOUSE_LENGTH = 35;   // Length of the house in feet

// Calculate the dimensions of the exterior walls
const WALL_WIDTH = HOUSE_WIDTH + 1;       // Add 1-foot buffer for exterior walls
const WALL_LENGTH = HOUSE_LENGTH + 1;     // Add 1-foot buffer for exterior walls

// Draw the exterior walls
drawExteriorWalls(WALL_WIDTH, WALL_LENGTH);
